[tool.poetry]
name = "yt-oop"
version = "0.1.0"
description = ""
authors = ["Damilola"]
readme = "README.md"
packages = [{include = "yt_oop"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
